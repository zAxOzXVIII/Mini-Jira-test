# Copia este archivo a `.env` y ajusta credenciales.
# Prisma 7 lee la URL desde `prisma.config.ts` vía esta variable.

DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/mini_jira?schema=public"
